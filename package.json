{
  "name": "calendar-assistant",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix backend && npm install --prefix app && npm run build --prefix app",
    "start": "node backend/dist/index.js"
  }
}
